import styled from 'styled-components';

const Wrapper = styled.div.attrs(() => ({ className: 'home__search' }))`
	display: flex;
	width: 100%;
	height: 4em;
	position: relative;
`;

export { Wrapper };
